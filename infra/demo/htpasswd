# Per-prospect basic-auth credentials. Managed by infra/demo/htpasswd-add.sh.
#
# Caddy only reads this file when the Caddyfile uses
#   basic_auth { import_file /etc/caddy/htpasswd }
# instead of the default inline single-user block. See HOSTED_DEMO.md §4.
#
# Format: one entry per line —  <username> <bcrypt-hash>
